import { useQueryClient } from "@tanstack/react-query";

const RestockProductKey = ["restock-product"];

export const useSeleectedRestockProduct = () => {
  const queryClient = useQueryClient();

  const addProduct = () => {
    // INSERT ADD RESTOCK PRODUCT LOGIC
  };
};
